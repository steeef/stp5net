server {
    server_name www www.stp5.net stp5.net;

    error_page 404 /404.html;

    if ($request_method !~ ^(GET|HEAD|POST)$ ) {
        return 444;
    }
    limit_req zone=one burst=5;

    location ~ ^/(favicon.ico$|apple-touch-icon) {
        empty_gif;
    }

    root /srv/www;
}
